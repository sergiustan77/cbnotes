import React from "react";

type Props = {};

const loading = (props: Props) => {
  return (
    <div className="container w-full h-[100vh] grid place-items-center bg-green-200">
      Loading...
    </div>
  );
};

export default loading;
