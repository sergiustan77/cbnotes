import { NavMenuDesktop } from "@/components/menus/NavMenuDesktop";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="">
      <NavMenuDesktop />
    </div>
  );
};

export default page;
